{% extends 'default/admin.html.twig' %}

{% block contenu %}
    <h1>Bluray creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_bluray_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
